baseURL = "/"
languageCode = "en-gb"
title = "SpareSlant Technical Blog"
theme = "mainroad"

paginate = "10"

[Author]
    name = "SpareSlant"

[Params]
    subtitle = "SpareSlant tech blog" # Subtitle of your site. Used in site header
    description = "SpareSlant tech blog" # Site description. Used in meta description
    copyright = "SpareSlant" # copyright holder, otherwise will use site title
    readmore = true # Show "Read more" button in list if true
    authorbox = true # Show authorbox at bottom of pages if true
    toc = true # Enable Table of Contents
    post_navigation = true # Show post navigation at bottom of pages if true
    post_meta = ["date", "categories", "translations"] # Order of post meta information
    # mainSections = ["post", "blog", "news"] # specify section pages to show on home page and the "Recent articles" widget
    dateformat = "2006-01-02" # change the format of dates
    customCSS = ["css/my.css"] # Include custom CSS files

[Params.sidebar]
    home = "right" # Configure layout for home page
    list = "left"  # Configure layout for list pages
    single = false # Configure layout for single pages
    # Enable widgets in given order
    widgets = ["search", "recent", "categories", "taglist", "social", "languages"]

[Params.widgets]
    recent_num = 5 # Set the number of articles in the "Recent articles" widget
    tags_counter = true # Enable counter for each tag in "Tags" widget (disabled by default)



